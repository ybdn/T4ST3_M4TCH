import React from "react";

// Stub temporaire pour réduire les erreurs de compilation liées aux anciennes dépendances.
// TODO: Réimplémenter avec nouvelle API de recherche / structure normalisée.

const ExternalSearchResults: React.FC = () => (
  <div className="p-4 text-sm text-tm-text-secondary bg-tm-surface-light rounded-lg">
    Recherche externe désactivée temporairement.
  </div>
);

export default ExternalSearchResults;
